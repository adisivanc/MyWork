const apis = ['https://url.io/js', 'https://urlhd.io/js', 'https://urlpp.io/js']; 

// Method type - GET

// #### Desired output ####
// resp = ['Resp-1', 'Resp-2', 'Err-3'];
// console.log(resp);

// solution
let resp = new Array(apis.length);

for(i=0;i<apis.length;i++){
    resp[i] = fetch(apis[i]).then(resp => {

    }).catch(err => {
        
    })
    // resp[i]=apis[i]
}

console.log(resp);

